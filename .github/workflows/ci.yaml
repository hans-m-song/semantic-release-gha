name: CI

on:
  - push

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "14"
      - run: npm install -g yarn
      - run: yarn install
      - run: semantic-release -d
